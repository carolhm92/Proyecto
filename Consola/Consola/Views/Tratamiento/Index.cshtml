@model IEnumerable<Consola.Models.Tratamiento>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NombreTratamiento)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Costo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IdMascota)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NombreTratamiento)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Costo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IdMascota)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdTratamiento }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdTratamiento }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdTratamiento })
        </td>
    </tr>
}

</table>
